{
  "name": "react-with-router-ssr-redux",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.15.3",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-redux": "^5.0.5",
    "react-router": "^4.1.2",
    "react-router-dom": "^4.1.2",
    "react-scripts": "1.0.10",
    "redux": "^3.7.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "build-server": "NODE_ENV='production' babel src --out-dir server --plugins=transform-es2015-modules-commonjs --presets=react-app",
    "run-server": "node server/server",
    "run": "yarn build && yarn build-server && yarn run-server",
    "clean": "rm -rf build server"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.24.1",
    "babel-preset-react-app": "^3.0.1"
  }
}
